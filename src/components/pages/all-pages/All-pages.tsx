import React from 'react';
import { withRouter } from 'react-router-dom';

const AllPages = () => (
  <div>
    <h1>All pages</h1>
    <h2>All pages</h2>
  </div>
);
export default withRouter(AllPages);
